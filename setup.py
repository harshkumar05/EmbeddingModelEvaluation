from setuptools import setup, find_packages

setup(
    name="LLMEvaluation",
    version="0.1.0",
    # This automatically finds 'projectUtils', 'utils', 'tests', etc.
    packages=find_packages(),
)